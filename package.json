{
  "name": "ionic-weather",
  "private": true,
  "version": "0.0.1",
  "description": "ionic-weather",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch",
    "generate": "stencil generate"
  },
  "devDependencies": {
    "@ionic/core": "^5.3.2",
    "@stencil/core": "2.8.0",
    "@types/jest": "^26.0.24",
    "jest": "^26.6.3",
    "jest-cli": "^26.6.3",
    "puppeteer": "^10.0.0"
  },
  "dependencies": {
    "@capacitor/cli": "^3.2.2",
    "@capacitor/core": "^3.2.2",
    "@capacitor/ios": "^3.2.2"
  }
}
